spring.application.name=kanbanflow

# MongoDB Config (Default 27017, database kanbanflow)
spring.data.mongodb.uri=mongodb://localhost:27017/kanbanflow

# Server config
server.port=8080

# CORS will be handled globally in config
